name: Generate Response

on:
  workflow_dispatch:

jobs:
  generate-response:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      # - name: Install dependencies
      #   run: npm install

      - name: chatgpt
        id: chatgpt
        uses: kiran-godse/chatdocument@main
        with:
          prompt: "what is github action"
          api_key: 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
